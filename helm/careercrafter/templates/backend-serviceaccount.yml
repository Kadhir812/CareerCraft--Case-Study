apiVersion: v1
kind: ServiceAccount

metadata:
  name: {{ .Values.backend.serviceAccount.name }}
  {{- if .Values.backend.serviceAccount.roleArn }}
  annotations:
    eks.amazonaws.com/role-arn: {{ .Values.backend.serviceAccount.roleArn }}
  {{- end }}
